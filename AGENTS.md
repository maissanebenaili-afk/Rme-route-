## Autonoma test data

Autonoma seeds realistic test data through this application's endpoint factories so end-to-end scenarios exercise the same creation paths as users. When models or their creation code change, add or update the matching factory and its recipe records so the test data remains aligned with the application.